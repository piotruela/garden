import 'package:flutter/material.dart';

class PlantListListTile extends StatelessWidget {
  const PlantListListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
